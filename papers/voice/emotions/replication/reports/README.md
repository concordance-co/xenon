# Reports

Local reports and small summaries for the paper replication can live here.

TODO: Add report subdirectories after runs, for example:

- `story_smoke/`
- `vector_space/`
- `geometry/`
- `heldout_scores/`
- `steering_controls/`

Do not store raw generations, activation dumps, or full generated datasets in
this directory.

