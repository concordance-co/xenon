# DX_TERMINAL

DX_TERMINAL is the umbrella project for Terminal Markets interpretability work.

The live repo keeps only the active prompt-confusion work here. Older DX
Terminal subprojects and historical phases were moved to the archive repo.

Active checked-in workflow entrypoints live under:

```text
projects/DX_TERMINAL/prompt_confusion/<phase>/specs/workflow.py
```
