[project]
name = "xenon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.3",
    "aiosqlite>=0.22.1",
    "pyarrow>=23.0.1",
]

[project.optional-dependencies]
interp = [
    "torch>=2.4.0",
    "transformers>=4.51.0",
    "safetensors>=0.4.0",
]
dev = [
    "pytest>=8.0",
]
